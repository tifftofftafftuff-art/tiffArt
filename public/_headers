# Cloudflare Workers static assets honors this file.
# /_astro/* filenames are content-hashed by Astro, so they can be cached
# forever — any change produces a new URL.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
